# Checkpoint mô hình (không commit vào Git)

Đặt các file sau vào thư mục này trước khi chạy inference:

| File | Bắt buộc |
|------|----------|
| `best_Attunet_efficientnet_b3.pth` | Có |
| `classifier_best.pth` | Có |
| `thresholds_seg.npy` | Không (mặc định 0.5) |
| `thresholds_cls.npy` | Không (mặc định 0.5) |

Chi tiết: xem `Readme.md`.
